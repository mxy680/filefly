{
	"name": "api",
	"version": "1.0.0",
	"description": "FastAPI application containerized with Docker and managed with Poetry.",
	"scripts": {
	  	"build": "poetry run prisma generate --schema=./prisma/schema.prisma",
    	"dev": "poetry run uvicorn apps.api.app.main:app --host 0.0.0.0 --port 8000"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
  }
  