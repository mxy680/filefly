{
	"name": "api",
	"version": "1.0.0",
	"description": "FastAPI application containerized with Docker and managed with Poetry.",
	"scripts": {
	  "dev": "poetry run python bootstrap/init.py && poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
  }
  