{
  "name": "rabbitmq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker-compose up --build -d",  
    "stop": "docker-compose down",           
    "restart": "npm run stop && npm run start" 
  }
}
